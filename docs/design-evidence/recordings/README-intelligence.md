# The presentation stage and intelligence recordings

Captured from the Playwright suite (video on, 360x740), the real engine
driving every step exactly as a judge would.

- `story-stage-presentation.webm` — a story runs watch only: the app under
  the caption is inert (a tap on the pay button does nothing), then the
  final step unlocks it and the sheet responds. From `demo.spec.ts`.
- `story-exits-matrix.webm` — the shelf exit clicked from every step of every
  signed in story. From `exits.spec.ts`.
- `intel-door1-arrival.webm` — how Svika knows your arrival: the live map's
  arrival number with its measured basis, then the honest ladder page with
  the committed evaluation table. From `intelligence.spec.ts`.
- `intel-door2-takunda.webm` — Takunda's alert, spine 2 at work: the learned
  routine fused with the live feed against the alarm clock baseline. From
  `stories.spec.ts`.
- `intel-door3-watchdog.webm` — the watchdog catches a leak: the bad day
  injected live, the fixed threshold rule silent while the isolation forest
  flags, closing on the bilingual narrative that names a pattern, never a
  person. From `intelligence.spec.ts`.
